FROM python:3

ADD Credentials.py /
ADD src/JenkinsUtils.py /
ADD requirements.txt /


RUN pip install -r requirements.txt
RUN python  --version
RUN echo "PythonDockerfile container is ready"
ENTRYPOINT ["/bin/bash"]


